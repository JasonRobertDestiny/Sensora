# Aether MVP Backend - Vercel Serverless
# Minimal dependencies for Vercel deployment
# Heavy deps (rdkit, sentence-transformers) have graceful fallbacks in code

# Web Framework
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6
httpx>=0.26.0

# Data Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Data Processing
numpy>=1.26.0

# OpenAI (primary AI engine)
openai>=1.12.0

# Environment
python-dotenv>=1.0.0
